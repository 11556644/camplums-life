所有依赖必须下载在这个文件夹

项目必须有日志保证可观测性，以及决策记录

工程化与架构约束

为了这个平台未来能扩展为"多校区、多业务线"的长期基础设施，需要在架构与工程流程上预设约束（而不是"先写再重构"）。

> **当前阶段豁免（D-006）：** 原型采用单体模块化架构，模块间通过 service 层调用代替物理服务拆分。
> 代码边界按服务划分（api/orders、api/wallet、api/messages 等），但共享同一 Prisma Client 和数据库连接。
> 待流量和团队规模增长后，可物理拆分为独立服务。

> **Phase 1 原型豁免：** 以下要求超出单体原型范围，待正式上线后实施：
> - 分布式 Trace / APM 指标采集（单体应用暂不需要）
> - 蓝绿/灰度发布（Railway 平台托管，手动部署）
> - 数据分库分表（当前单校区，流量不足以分片）
> - 单元测试覆盖率门槛（已识别，待补充 vitest + 核心路径测试）

领域与服务边界约束：

至少抽出统一的"用户/权限服务""订单服务""支付结算服务""消息与通知服务""信用与风控服务"，业务线通过"订单类型 + 业务扩展表/扩展服务"接入。

禁止在业务线内直接依赖其他业务线的 DB，跨服务交互必须通过 API 或事件总线，避免耦合地狱。

多租户与多学校演进：

从第一天起数据模型中预留"学校/校区"维度，所有关键表按 school_id 进行隔离，保证未来可一库多校或多库多校灵活切换。

配置（规则、套餐、禁售品类）以"学校"为作用域，做到多校差异化可配置。

一致性与幂等约束：

所有对外接口（支付回调、IoT 开柜回调、消息推送回调）必须幂等设计，有全局唯一业务号与状态机保护。

订单与资金变动必须通过统一的状态机 + 事件日志驱动，禁止在业务代码中直接"随手 UPDATE 金额"。

可观测性与审计：

默认所有服务都必须输出结构化日志、指标（QPS、错误率、耗时）和分布式 Trace，方便定位 IoT/支付/业务问题。

所有涉及账号变更、权限变更、资金与信用调整的操作，必须写入审计日志并长期保存，供合规检查。

运维、质量与发布约束

测试与质量门槛：

单元测试与接口测试覆盖关键领域（支付、订单状态机、库存变更、柜机控制），覆盖率达到约定阈值方可合入主干。

对 IoT 与支付流程必须有端到端的沙箱环境与联调脚本，避免线上联调。

发布与灰度：

采用蓝绿/灰度发布，对不同学校/校区分批放量，便于回滚和 AB 测试。

对安全敏感改动（权限、风控规则调整）强制走变更评审与回滚预案。

数据治理与备份：

业务数据、日志数据分库分表，定期备份与恢复演练，满足学校对数据留存期限及恢复能力的要求。

对历史订单与日志的数据脱敏与归档策略提前规划，避免库无限膨胀。
