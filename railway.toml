[build]
builder = "nixpacks"

[deploy]
startCommand = "mkdir -p data/uploads && npx prisma db push --accept-data-loss --skip-generate && npx next start -p $PORT"
healthcheckPath = "/"
restartPolicyType = "on_failure"
